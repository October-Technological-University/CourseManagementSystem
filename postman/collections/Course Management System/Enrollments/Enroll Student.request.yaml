$kind: http-request
name: Enroll Student
method: POST
url: '{{baseUrl}}/api/enrollments'
order: 5000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "student_id": "{{studentId}}",
      "course_id": "{{courseId}}"
    }
